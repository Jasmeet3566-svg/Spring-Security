# Spring Application Name
spring.application.name=springSecurityEx

# Spring Security user credentials (if you're using basic security for testing)
spring.security.user.name=Jasmeet
spring.security.user.password=1234

# DataSource configuration for MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/telusko1?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Jasmeet@123

# Optional: Driver class name (usually not required with Spring Boot)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Hibernate properties (if you're using JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
